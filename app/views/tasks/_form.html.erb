<%= form_for(@task) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :description %>
<%= f.text_field :description %>

  <div>
    <%= f.label "Done" %>
    <%= f.check_box :done %>
  </div>

<%= f.submit %>

<% end %>
